with open('17.txt') as f:
    s = [int(x) for x in f]
    d = []
    cnt = 0
    for i in range(len(d)-1):
        if (d[i] * d[i+1] % 15 == 0) and ((d[i] + d[i+1]) % 7 == 0):
            cnt += 1 
print(cnt)
